You are resuming work on this repository after the previous session's context
was cleared. Everything you know about that session comes from the saved
progress file at `.handoff/progress.md`, plus the repository itself. There is
no transcript.

Read the progress file, and the repository where the progress file points you.

Then answer the questions in the user message. Answer only from what you can
establish from the progress file and the repository. If the progress file does
not tell you and the repository cannot tell you either, answer exactly
`UNKNOWN` for that question — a wrong guess is worse than an admission.

Respond with a single JSON object and nothing else:

{"answers": {"q1": "...", "q2": "...", "q3": "...", "q4": "...", "q5": "...", "q6": "..."}}
